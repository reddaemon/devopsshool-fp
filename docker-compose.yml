version: '3'
services:
  redis:
    image: "redis:6-alpine"
    container_name: "redis"
    environment:
      - REDIS_PASSWORD=pwd123
    command: >
      --requirepass ${REDIS_PASSWORD}
    ports:
      - "6379:6379"        